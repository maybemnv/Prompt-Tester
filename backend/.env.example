# Environment Variables for AI Prompt Stress Tester Backend

# Port for the backend server (defaults to 5000)
# PORT=5000

# AWS Configuration for Bedrock (optional, if not using Bedrock, heuristic fallback will be used)
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Model configuration (optional, defaults to local/heuristic evaluation)
# MODEL_NAME=Qwen/Qwen2-72B-Instruct